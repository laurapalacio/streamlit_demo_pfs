# Get started
You first need the OpenAI key set up (without the ""):
```
export OPENAI_API_KEY=X
```

# Run the app locally

You can run it by installing dependencies first:
```
uv sync
```
Then, you can run:
```
uv run streamlit run main.py
```
